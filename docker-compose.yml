version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=${MONGODB_URI:-mongodb://root:8zoObOSvuToWFiUPcH1plbVDyuFgUCUT34pu8wB5aS9yp6sn2jmyFa9zEWEXCI6m@116.203.131.84:5433/?directConnection=true}
      - PORT=3000
      - DATABASE_NAME=schwudelrudel
    restart: unless-stopped
    networks:
      - schwudelrudel-network

networks:
  schwudelrudel-network:
    driver: bridge
